import { AboutContainer } from "@/modules/About";
import { FC } from "react";

const Container: FC = () => {
  return (
    <div>
      <AboutContainer />
    </div>
  );
};

export default Container;
