import { ServiceContainer } from "@/modules/Services";
import { FC } from "react";

const Container: FC = () => {
  return (
    <div>
      <ServiceContainer />
    </div>
  );
};

export default Container;
