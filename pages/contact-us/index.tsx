import { ContactContainer } from "@/modules/Contact";
import { FC } from "react";

const Container: FC = () => {
  return (
    <div>
      <ContactContainer />
    </div>
  );
};

export default Container;
